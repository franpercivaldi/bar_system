# Opcional en local: si no está definido, el cliente usa http://localhost:3000
# En Railway (build del frontend) es obligatorio apuntar al API público:
# VITE_API_URL=https://tu-backend.up.railway.app
